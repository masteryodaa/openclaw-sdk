# ── Claude Code workspace ──────────────────────────────────────────
# Ignore contents of .claude/ by default, then un-ignore team-shared files
# MUST use .claude/* (not .claude/) so git looks inside and negation works
.claude/*
!.claude/CLAUDE.md
!.claude/compaction-context.md
!.claude/settings.json
!.claude/rules/
!.claude/rules/**
!.claude/skills/
!.claude/skills/**
!.claude/hooks/
!.claude/hooks/**
!.claude/tasks/
!.claude/tasks/**
!.claude/context/
!.claude/context/**

# Personal Claude files (never committed)
CLAUDE.local.md

# ── Planning docs (reference only) ────────────────────────────────
doc.md
plan.md
old_plan.md
plans/

# ── Virtual environment ───────────────────────────────────────────
env/
.venv/

# ── Poetry ────────────────────────────────────────────────────────
poetry.lock

# ── Python build artefacts ────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# ── Test / coverage artefacts ─────────────────────────────────────
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── MkDocs build output ──────────────────────────────────────────
site/

# ── Development screenshots ──────────────────────────────────────
*.png

# ── Scratch / temp files ─────────────────────────────────────────
gen_tests.py
test_attachment_types.py

# ── Playwright MCP logs ──────────────────────────────────────────
.playwright-mcp/
